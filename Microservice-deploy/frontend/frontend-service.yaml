apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: roboshop
spec:
  type: LoadBalancer   # So you can access from browser
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
